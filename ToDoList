/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 */

package com.mycompany.todolist;

import java.util.ArrayList;
import java.util.Collections;
/**
 *
 * @author KVadukoot2026
 */
public class ToDoList {

    public static void main(String[] args) {
        
        task t1, t2, t3, t4, t5;
       //List of tasks for the user 
       
       //First task
        t1 = new task("Doing laundry");
        //How important is the task?
        t1.setPriority(3);
        //How hard is the task?
        t1.setComplexity(2);
        
        //Repeat for the next 4 tasks
        
        
        //Task 2
        t2 = new task("Study for test");
        t2.setPriority(1);
        t2.setComplexity(4);
        
        //Task 3
        t3 = new task("Make dinner");
        t3.setPriority(5);
        t3.setComplexity(3);
        
        //Task 4
        t4 = new task("Do homework");
        t4.setPriority(2);
        t4.setComplexity(3);
        
        //Task 5
        t5 = new task("Walk the dog");
        t5.setPriority(4);
        t5.setComplexity(1);
        
        //Make an array list for the tasks
        ArrayList<task> tasks = new ArrayList<>();
        //Add each of the tasks
        tasks.add(t1);
        tasks.add(t2);
        tasks.add(t3);
        tasks.add(t4);
        tasks.add(t5);
        
        Collections.sort(tasks);
        
        System.out.println("The tasks will be sorted by priority and complexity. This is the list: ");
                for (task t : tasks){
                    System.out.println(t.getTask() + " (Priority: " + t.getPriority() + 
                            ", Complexity: " + t.getComplexity() + ")");
                }
        
        
        
        
    }
}
//Create another class
    class task implements Priority, Complexity, Comparable <task>{
        //Make a private string and private integers
        private String task;
        private int priorityLevel;
        private int complexityLevel;
        
        public task(String taskName) {
            task = taskName;
            priorityLevel = 1;
            complexityLevel = 1;
        }
        
        @Override
        public void setPriority(int level){
            priorityLevel = level;
        }
        
        @Override
        public int getPriority(){
            return priorityLevel;
        }
        
        @Override
        public void setComplexity(int level){
            complexityLevel = level;
        }
        
        @Override
        public int getComplexity(){
            return complexityLevel;
    }
        public String getTask(){
            return task;
        }
        
        @Override
        public int compareTo(task other){
            if (this.priorityLevel != other.priorityLevel)
                return this.priorityLevel - other.priorityLevel;
            else
                return this.complexityLevel - other.complexityLevel;
        }

}

interface Priority{
    public void setPriority(int priority);
    public int getPriority();
}

interface Complexity{
    public void setComplexity(int complexity);
    public int getComplexity();
}
