/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package contactsassignment;

import java.io.BufferedWriter;
import java.io.FileWriter;
import java.io.IOException;
import java.util.Arrays;
import java.util.List;
import java.util.ArrayList;


/**
 *
 * @author KVadukoot2026
 */
public class ContactsAssignment {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        String csvFilePath = "files/example.csv";
       ArrayList<Contact> contacts = new ArrayList<>();

       loadContacts(csvFilePath, contacts);

       Scanner scanner = new Scanner(system.in);

       while(true) {
       System.out.println("\nEnter command:");
       System.out.println("a = create new contact");
       System.out.println("e = sort by email adress");
       System.out.println("y = sort by grad year");
       System.out.println("n = sort by name");
       System.out.println("q = quit");
        
       String input = scanner.nextLine();

        if (input.equals("a")){
            System.out.print("Enter name: ");
            String name = scanner.nextLine();
}
           }
        }
    }

    

 public static void addAges(List<List<String>> data){
        List<String> line;
        int totalAge = 0;
        //start counting at index 1 - data first line is headers
        for(int i = 1; i < data.size(); i++){
            line = data.get(i);
            totalAge = totalAge + Integer.parseInt(line.get(1));
            System.out.println(line);
        }
        System.out.println(totalAge);
    }
    //writes the data List to a CSV (including the column headers)
    public static void writeCsvFile(String filePath, List<List<String>> data) throws IOException {
        try (BufferedWriter writer = new BufferedWriter(new FileWriter(filePath))) {
            for (List<String> row : data) {
                String line = String.join(",", row);
                writer.write(line);
                writer.newLine();
            }
        }
    }
    
    public static ArrayList getPeople(List<List<String>> data){
        //Convert the List of Lists to an ArrayList of Person objects
        ArrayList people = new ArrayList();
        //start counting at index 1 - data first line is headers
        for(int i = 1; i < data.size(); i++){
            Person p = new Person(data.get(i));
            people.add(p);
        }
        return people;
    }


//the class for the Persons object
class Person{
    private String name = "";
    private int age = 0;
    private String city = "";
    //empty constuctor
    public Person(){}
    
    //constructor that takes a List of Strings
    public Person(List<String> inPerson){
        name = inPerson.get(0);
        age = Integer.parseInt(inPerson.get(1));
        city = inPerson.get(2);
    }
    
    public String getName(){
        return name;
    }
    
    public String getCity(){
        return city;
    }
    
    public int getAge(){
        return age;
    }
}
    
